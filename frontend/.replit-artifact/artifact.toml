kind = "web"
previewPath = "/"
title = "AgriNova AI"
version = "1.0.0"
id = "artifacts/agrinova"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19612

[services.development]
run = "pnpm --filter @workspace/agrinova run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/agrinova", "run", "build" ]
publicDir = "artifacts/agrinova/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19612"
BASE_PATH = "/"
